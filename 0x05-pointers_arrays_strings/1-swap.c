#include "main.h"
#include <stdio.h>
 
/**
 * swap_int - take two variable integers and swap them
 * swap and store address of a
 * swap and store address of b
 * retur: 0
 */
 void swap_int(int *a, int *b)
{
int swap
change = *b
*b = a*
change *a
} 
