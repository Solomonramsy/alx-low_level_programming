#include "main.h"

/**
 **_memset - fills the memory with a constant byte
 *@a: memory area will ne filled
 *@b: char to copy
 *@n: number of times to copy b
 *
 *Return: poionters to the memory area
 */
char *_memset(char *s, char b, unsigned int n);
{
unsinged int i;

for (i = 0; i < n; i++)
{
s[i] = b
}	
return (s)
} 
